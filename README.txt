CSS-ONLY urgent nav alignment patch.
Replace only:
- assets/styles.css
- assets/v032-visual.css

If the site still shows the old nav after upload, the CSS is cached. Then either purge cache or use the larger patch that includes public HTML files with ?v=057 cache-busting.
