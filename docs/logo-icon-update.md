# Logo/Icon Update

This package adds the approved LOC-style plat map icon to AllottedLand.com.

Added files:

- `assets/icons/allottedland-logo-icon.png`
- `assets/icons/favicon.ico`
- PNG favicon sizes, Apple touch icon, Android/PWA icons
- `assets/icons/site.webmanifest`

Updated files:

- HTML pages now include favicon / app icon metadata in the `<head>`.
- Public header brand links now display the icon before the existing `AllottedLand.com` text without changing the site wording.
- `assets/styles.css` and `assets/v032-visual.css` include `.brand-logo-icon` styling.
